/**
 * @author harlin
 * 用戶故事 US-5.13.3（小程序 · 待接入自動化）
 * 作为会员，我希望支付成功后订单进入待制作、清预扣并扣减真实库存，以便门店备货且账实一致。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.13.3 支付成功后订单进入待制作、清预扣并扣减真实库存', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
