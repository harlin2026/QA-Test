/**
 * @author harlin
 * 用戶故事 US-5.15.2（小程序 · 待接入自動化）
 * 作为会员，我希望商品列表的最优券推荐按「优惠金额→最快到期→券类型」规则排序，以便优先看到对我最划算且快过期的券。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.15.2 商品列表的最优券推荐按「优惠金额→最快到期→券类型」规则排序', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
