/**
 * @author harlin
 * 用戶故事 US-5.9.1（小程序 · 待接入自動化）
 * 作为会员，我希望门店支持一天多个营业时间段，以便在真实营业时段选择自取。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·營業時段', () => {
  test('US-5.9.1 门店支持一天多个营业时间段', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
