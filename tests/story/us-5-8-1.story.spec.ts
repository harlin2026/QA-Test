/**
 * @author harlin
 * 用戶故事 US-5.8.1（小程序 · 待接入自動化）
 * 作为会员，我希望登录小程序时授权手机号，以便完成身份绑定与后续下单履约。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·授權', () => {
  test('US-5.8.1 登录小程序时授权手机号', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
