/**
 * @author harlin
 * 用戶故事 US-5.16.2（小程序 · 待接入自動化）
 * 作为会员，我希望确认订单自动计算促销活动优惠、打包费（自提无服务费），并看到优惠金额与应付金额。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.16.2 确认订单自动计算促销活动优惠、打包费（自提无服务费），并看到优惠金额与应…', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
