/**
 * @author harlin
 * 用戶故事 US-5.11.1（小程序 · 待接入自動化）
 * 作为会员，我希望确认订单时看到可用与不可用券列表及不可用原因，以便选择优惠。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.11.1 确认订单时看到可用与不可用券列表及不可用原因', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
