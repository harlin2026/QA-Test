/**
 * @author harlin
 * 用戶故事 US-5.10.2（小程序 · 待接入自動化）
 * 作为会员，我希望按「一级分类名+其下商品」「二级分类名+其下商品」层级浏览。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.10.2 按「一级分类名+其下商品」「二级分类名+其下商品」层级浏览。', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
