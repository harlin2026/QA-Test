/**
 * @author harlin
 * 用戶故事 US-5.14.1（小程序 · 待接入自動化）
 * 作为会员，我希望在优惠券列表看到每张券的适用门店信息，以便判断该券能否在我要下单的门店使用。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.14.1 在优惠券列表看到每张券的适用门店信息', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
