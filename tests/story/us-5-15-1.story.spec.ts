/**
 * @author harlin
 * 用戶故事 US-5.15.1（小程序 · 待接入自動化）
 * 作为会员，我希望看到分类图标、富文本和标签（后台可开关），以便更直观地浏览菜单。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.15.1 看到分类图标、富文本和标签（后台可开关）', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
