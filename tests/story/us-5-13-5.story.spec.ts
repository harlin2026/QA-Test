/**
 * @author harlin
 * 用戶故事 US-5.13.5（小程序 · 待接入自動化）
 * 作为会员，我希望取消待支付订单或订单超时取消时库存被退回，以便库存可被他人购买。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.13.5 取消待支付订单或订单超时取消时库存被退回', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
