/**
 * @author harlin
 * 用戶故事 US-5.16.1（小程序 · 待接入自動化）
 * 作为会员，我希望在购物车/确认订单里按套餐组选购套餐，以便享受套餐促销。
 */
import { test } from '../fixtures/base-test';

test.describe('小程序·登入會員', () => {
  test('US-5.16.1 在购物车/确认订单里按套餐组选购套餐', async () => {
    test.skip(true, '小程序端尚未接入 Playwright；故事已入庫，待接入後補齊自動化步驟');
  });
});
